import React, { FC } from "react";
import styles from "./style.module.scss";

const Footer: FC = () => (
  <footer className={styles.footer}>
    <div className={styles.inner}>©2021 Twiv</div>
  </footer>
);

export default Footer;
